<div class="row" >
	<div class="col-xs-8 col-xs-offset-2" >
		<h1 align="center"> Showing selected medicine</h1>
	</div>
</div>

<p>
	<h2 align="center">Title: <%= @medicine.title %></h2>
</p>
<body style=" background-image: url('/assets/show.jpg'); background-size: cover;">

<div class="well col-xs-8 col-xs-offset-2">
	
	<!--<ul class="listing">
			<div class="row">
				<div class="well col-md-4 col-md-offset-4">
					<li> <%= link_to gravatar_for(@medicine.user), user_path(@medicine.user) %></li>
						<li class="medicine-title">
							<%= link_to @medicine.user.username, user_path(@medicine.user) %>
						</li>
						<li><small> <%= pluralize(@medicine.user.medicines.count, "medicine") if @medicine.user.medicines %> </small></li>
					</div>
				</div>
			</ul> -->

	<h4 class="center"><strong>Description:</strong></h4>
	<hr>
		<%= simple_format(@medicine.description) %>
	</p>
	<p>
		Company: <%= @medicine.company %>
	</p>
	<p>
		Batch_Number: <%= @medicine.batch_number %>
	</p>
	<p>
		Manufacturing_Date: <%= @medicine.manufacturing_date %>
	</p>
	<p>
		Expiry_Date: <%= @medicine.expiry_date %>
	</p>

	<div class="medicine-actions">
		<%= link_to "Edit this medicine details", edit_medicine_path(@medicine), class: "btn btn-xs btn-primary" %> 
		<%= link_to "Remove this medicine", medicines_path(@medicine), method: :delete,
		 data: {confirm: "Are you sure you want to delete this medicine?"}, class: "btn btn-xs btn-danger" %>
		<%= link_to "View all Medicines", medicines_path, class: "btn btn-xs btn-success" %>
	</div>
</div>